# Website Images

Add the following images to this folder:

- `favicon.png` - 32x32 favicon
- `hero-dashboard.png` - Dashboard screenshot for hero section (optional)

The website uses CSS-based mockups by default, so images are optional.
